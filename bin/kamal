#!/usr/bin/env bash
# Loads .env (gitignored) before invoking the real kamal binary, so
# config/deploy.yml can read DEPLOY_HOST and friends via ENV.fetch.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

if [ -f .env ]; then
  set -a
  source .env
  set +a
fi

exec kamal "$@"
