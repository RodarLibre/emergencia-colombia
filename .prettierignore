.next
node_modules
coverage
pnpm-lock.yaml
tsconfig.tsbuildinfo
public

# Sanitized real-page fixtures — reformatting would diverge them from the
# actual source structure they're meant to mimic.
fixtures/

# Generated by scripts/fetch-municipios.mjs with compact (indent 0) JSON;
# reformatting here would just make every regeneration dirty the diff again.
src/lib/data/municipios.json

# Prose docs, not code — left to their authors' formatting.
*.md
docs/

# Agent worktrees: full copies of this repo inside it. They must never be
# committed, shipped to the Docker build context, or formatted.
.claude/worktrees/
